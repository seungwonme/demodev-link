제공된 코드베이스는 기능별 폴더 구조(Features Directory Structure)를 잘 따르고 있어 기본 구조는 훌륭합니다. 다만, **UI 컴포넌트의 비대화(Logic/View 혼재)**, **에러 처리의 일관성 부족**, **타입 검증(Validation) 로직의 분산** 등의 개선점이 보입니다.

가독성, 유지보수성, 테스트 용이성을 높이기 위한 리팩터링 제안과 적용 단계를 설명합니다.

---

### 🚀 리팩터링 전략

**1단계: 기반 다지기 (Foundation)**
*   **Result Pattern 도입:** 성공/실패 응답의 타입을 표준화하여 에러 처리를 일관되게 만듭니다.
*   **Zod 스키마 도입:** Server Action에서 수동으로 하던 유효성 검사를 Zod로 중앙화하여 안정성을 높입니다.

**2단계: 비즈니스 로직 분리 (Logic Extraction)**
*   **Server Action 리팩터링:** `shortenUrl` 등의 액션에 Validation 및 에러 핸들링 표준을 적용합니다.

**3단계: UI 컴포넌트 분리 (Component Decomposition)**
*   **LinkList 분리:** 데이터 페칭, 상태 관리, 렌더링이 섞인 거대한 컴포넌트를 `LinkItem`과 `useLinkList` 훅으로 분리합니다.

---

### 1단계: 기반 다지기 (Foundation)

먼저, 일관된 응답 처리를 위한 유틸리티 타입과 유효성 검사 스키마를 정의합니다.

#### 1-1. Result 타입 정의 (신규 파일)
`src/shared/types/result.ts`를 생성하여 서버 액션의 응답 규격을 통일합니다.

```typescript
// src/shared/types/result.ts

export type Result<T, E = string> =
  | { success: true; data: T }
  | { success: false; error: E };

export const Result = {
  success: <T>(data: T): Result<T> => ({ success: true, data }),
  fail: <E = string>(error: E): Result<never, E> => ({ success: false, error }),
};
```

#### 1-2. Link 관련 Zod 스키마 정의 (신규 파일)
`src/features/links/schemas/link.schema.ts`를 생성하여 유효성 검사 로직을 중앙화합니다.

```typescript
// src/features/links/schemas/link.schema.ts
import { z } from "zod";

export const utmParamsSchema = z.object({
  utm_source: z.string().optional(),
  utm_medium: z.string().optional(),
  utm_campaign: z.string().optional(),
  utm_term: z.string().optional(),
  utm_content: z.string().optional(),
});

export const createLinkSchema = z.object({
  original_url: z.string().url({ message: "유효한 URL 형식이 아닙니다." }),
  custom_slug: z
    .string()
    .regex(/^[a-zA-Z0-9-]+$/, "영문, 숫자, 하이픈만 사용 가능합니다.")
    .max(50, "50자를 초과할 수 없습니다.")
    .optional()
    .or(z.literal("")),
  description: z.string().max(500).optional(),
  utm_params: utmParamsSchema.optional(),
});

export type CreateLinkInput = z.infer<typeof createLinkSchema>;
```

---

### 2단계: 비즈니스 로직 리팩터링

기존 Server Action을 리팩터링하여 위에서 만든 스키마와 Result 패턴을 적용합니다. 이를 통해 코드의 안정성을 높이고 테스트하기 쉽게 만듭니다.

#### 2-1. `shorten-url.ts` 리팩터링

```typescript
// src/features/links/actions/shorten-url.ts
"use server";

import { LinkService } from "@/features/links/actions/link.service";
import { LinkResponse } from "@/shared/types/link";
import { ClerkAuthService } from "@/features/auth/services/clerk-auth.service";
import { createLinkSchema, CreateLinkInput } from "../schemas/link.schema";
import { Result } from "@/shared/types/result";

// 기존 함수 시그니처 변경 없이 내부 로직 강화 또는 새로운 함수 추가
// 여기서는 리턴 타입을 명확한 Result 패턴으로 변경한 버전을 제안합니다.

export async function shortenUrlAction(
  input: CreateLinkInput
): Promise<Result<LinkResponse>> {
  try {
    // 1. 입력값 검증 (Zod)
    const validatedData = createLinkSchema.safeParse(input);
    if (!validatedData.success) {
      return Result.fail(validatedData.error.errors[0].message);
    }

    // 2. 권한 확인
    const user = await ClerkAuthService.requireAuth({ requiredStatus: "approved" });

    // 3. 서비스 호출
    const link = await LinkService.createShortLink(
      validatedData.data,
      undefined,
      user.userId
    );

    const shortUrl = `${process.env.NEXT_PUBLIC_BASE_URL}/${link.slug}`;

    return Result.success({
      shortUrl,
      originalUrl: link.original_url,
    });
  } catch (error) {
    console.error("Error shortening URL:", error);
    if (error instanceof Error) {
      // 사용자에게 보여줄 수 있는 에러인지 구분이 필요하지만,
      // LinkService에서 명시적으로 던진 에러는 메시지를 전달합니다.
      return Result.fail(error.message);
    }
    return Result.fail("URL을 단축하는 중 알 수 없는 오류가 발생했습니다.");
  }
}

// 호환성을 위해 기존 함수는 유지하되 내부에서 새로운 로직을 호출하거나 유지
export async function shortenUrl(data: any) {
    const result = await shortenUrlAction(data);
    if (!result.success) throw new Error(result.error);
    return result.data;
}
```

---

### 3단계: UI 컴포넌트 분리 (Component Decomposition)

`LinkList` 컴포넌트는 현재 너무 많은 역할을 하고 있습니다. 이를 **단일 책임 원칙(SRP)**에 따라 분리합니다.

1.  `LinkItem`: 개별 링크 아이템을 렌더링하는 순수 컴포넌트
2.  `LinkList`: 목록을 순회하며 `LinkItem`을 그리는 컨테이너

#### 3-1. `LinkItem` 컴포넌트 분리 (src/features/links/components/link-item.tsx)

```typescript
"use client";

import { Link } from "@/shared/types/link";
import { Card, CardContent } from "@/shared/components/ui/card";
import { Button } from "@/shared/components/ui/button";
import { Youtube, Copy, ExternalLink, Edit2, FolderPlus, BarChart3, Trash2 } from "lucide-react";
import { isYouTubeUrl, getYouTubeVideoId } from "../utils/youtube";
import { YouTubeThumbnail } from "./youtube/youtube-thumbnail";
import { YouTubeEmbed } from "./youtube/youtube-embed";
import { useState } from "react";
import {
  AlertDialog,
  AlertDialogAction,
  AlertDialogCancel,
  AlertDialogContent,
  AlertDialogDescription,
  AlertDialogFooter,
  AlertDialogHeader,
  AlertDialogTitle,
  AlertDialogTrigger,
} from "@/shared/components/ui/alert-dialog";

interface LinkItemProps {
  link: Link & { period_clicks?: number };
  baseUrl: string;
  isDateRangeAll: boolean;
  onDelete: (id: string) => void;
  onUpdateDescription: (id: string, description: string) => void;
  onCopy: (text: string) => void;
  onShowStats: (link: Link) => void;
  onAddToCampaign: (id: string) => void;
  renderStats?: React.ReactNode; // Stats 컴포넌트 주입
}

export function LinkItem({
  link,
  baseUrl,
  isDateRangeAll,
  onDelete,
  onUpdateDescription,
  onCopy,
  onShowStats,
  onAddToCampaign,
  renderStats,
}: LinkItemProps) {
  const [expandedYouTube, setExpandedYouTube] = useState(false);
  const [isEditing, setIsEditing] = useState(false);
  const [editDescription, setEditDescription] = useState(link.description || "");

  const handleSaveDescription = () => {
    onUpdateDescription(link.id, editDescription);
    setIsEditing(false);
  };

  const formatDate = (dateString: string | null) => {
    if (!dateString) return "";
    return new Date(dateString).toISOString().split("T")[0];
  };

  return (
    <Card className="border-none hover:bg-muted/50 dark:hover:bg-white/5 shadow-sm transition-all">
      <CardContent className="p-4">
        <div className="flex justify-between items-start">
          <div className="flex-1 mr-4">
            <div className="flex items-center gap-3">
              {isYouTubeUrl(link.original_url) && (
                <YouTubeThumbnail
                  videoId={getYouTubeVideoId(link.original_url) || ""}
                  size="sm"
                  onClick={() => setExpandedYouTube(!expandedYouTube)}
                />
              )}
              <div className="flex-1">
                <div className="flex items-center gap-2">
                  {isYouTubeUrl(link.original_url) && (
                    <Youtube className="h-4 w-4 text-red-600 flex-shrink-0" />
                  )}
                  <p className="font-medium break-all line-clamp-1">{link.original_url}</p>
                </div>
              </div>
            </div>

            {isEditing ? (
              <div className="mt-2 flex items-center gap-2">
                <input
                  type="text"
                  value={editDescription}
                  onChange={(e) => setEditDescription(e.target.value)}
                  className="flex-1 px-2 py-1 text-sm border border-border rounded bg-background text-foreground focus:outline-none focus:ring-2 focus:ring-primary/50"
                  placeholder="설명을 입력하세요"
                  onKeyDown={(e) => {
                    if (e.key === "Enter") handleSaveDescription();
                    else if (e.key === "Escape") setIsEditing(false);
                  }}
                />
                <Button size="sm" onClick={handleSaveDescription}>저장</Button>
                <Button size="sm" variant="ghost" onClick={() => setIsEditing(false)}>취소</Button>
              </div>
            ) : (
              <div className="flex items-center gap-2 mt-1">
                {link.description && (
                  <p className="text-sm text-muted-foreground">{link.description}</p>
                )}
                <Button size="sm" variant="ghost" className="h-6 w-6 p-0" onClick={() => setIsEditing(true)}>
                  <Edit2 className="h-3 w-3" />
                </Button>
              </div>
            )}

            <div className="flex items-center gap-2 mt-2">
              <p className="text-sm text-muted-foreground">
                {baseUrl}/{link.slug}
              </p>
              <Button size="sm" variant="ghost" onClick={() => onCopy(`${baseUrl}/${link.slug}`)}>
                <Copy className="h-3 w-3" />
              </Button>
              <Button
                size="sm"
                variant="ghost"
                onClick={() => window.open(`${baseUrl}/${link.slug}`, "_blank")}
              >
                <ExternalLink className="h-3 w-3" />
              </Button>
            </div>
          </div>

          <div className="text-right">
            {isDateRangeAll ? (
              <p className="font-medium">{link.click_count} 클릭</p>
            ) : (
              <>
                <p className="font-medium text-primary">{link.period_clicks || 0} 클릭</p>
                <p className="text-xs text-muted-foreground">(전체: {link.click_count})</p>
              </>
            )}
            <p className="text-sm text-muted-foreground mt-1">{formatDate(link.created_at || "")}</p>

            <div className="flex gap-2 mt-2 justify-end">
              <Button size="sm" variant="outline" onClick={() => onAddToCampaign(link.id)}>
                <FolderPlus className="h-4 w-4" />
              </Button>
              <Button size="sm" variant="outline" onClick={() => onShowStats(link)}>
                <BarChart3 className="h-4 w-4" />
              </Button>
              <AlertDialog>
                <AlertDialogTrigger asChild>
                  <Button size="sm" variant="destructive">
                    <Trash2 className="h-4 w-4" />
                  </Button>
                </AlertDialogTrigger>
                <AlertDialogContent>
                  <AlertDialogHeader>
                    <AlertDialogTitle>링크 삭제</AlertDialogTitle>
                    <AlertDialogDescription>
                      이 링크를 삭제하시겠습니까? 삭제된 링크는 복구할 수 없습니다.
                    </AlertDialogDescription>
                  </AlertDialogHeader>
                  <AlertDialogFooter>
                    <AlertDialogCancel>취소</AlertDialogCancel>
                    <AlertDialogAction onClick={() => onDelete(link.id)}>삭제</AlertDialogAction>
                  </AlertDialogFooter>
                </AlertDialogContent>
              </AlertDialog>
            </div>
          </div>
        </div>

        {expandedYouTube && isYouTubeUrl(link.original_url) && (
          <div className="mt-4 border-t pt-4">
            <YouTubeEmbed
              videoId={getYouTubeVideoId(link.original_url) || ""}
              title={link.description || undefined}
              className="max-w-2xl"
            />
          </div>
        )}

        {renderStats && (
          <div className="mt-4 border-t pt-4">
            {renderStats}
          </div>
        )}
      </CardContent>
    </Card>
  );
}
```

#### 3-2. `LinkList` 리팩터링 (src/features/links/components/link-list.tsx)

```typescript
"use client";

import { useState, useEffect, useCallback } from "react";
import { Link } from "@/shared/types/link";
import { Card, CardContent } from "@/shared/components/ui/card";
import { Button } from "@/shared/components/ui/button";
import { deleteLink } from "@/features/links/actions/delete-link";
import { updateLinkDescription } from "@/features/links/actions/update-link";
import { getAllLinksForAnalytics } from "@/features/analytics/actions/get-analytics";
import LinkStats from "@/features/analytics/components/link-stats";
import { Calendar } from "lucide-react";
import { toast } from "sonner";
import { AddToCampaignDialog } from "@/features/campaigns/components/add-to-campaign-dialog";
import { LinkItem } from "./link-item";

interface LinkListProps {
  initialLinks: Link[];
}

type DateRangePreset = '7d' | '30d' | '3m' | '6m' | 'all' | 'custom';

export default function LinkList({ initialLinks }: LinkListProps) {
  const [links, setLinks] = useState<(Link & { period_clicks?: number })[]>(initialLinks);
  const [showAll, setShowAll] = useState(false);
  const [selectedLinkStatsId, setSelectedLinkStatsId] = useState<string | null>(null);
  const [baseUrl, setBaseUrl] = useState<string>("");

  // Date State
  const [dateRangePreset, setDateRangePreset] = useState<DateRangePreset>('all');
  const [customStartDate, setCustomStartDate] = useState<string>('');
  const [customEndDate, setCustomEndDate] = useState<string>('');

  // Dialog State
  const [showAddToCampaign, setShowAddToCampaign] = useState(false);
  const [selectedLinkForCampaign, setSelectedLinkForCampaign] = useState<string | null>(null);

  useEffect(() => {
    setBaseUrl(window.location.origin);
  }, []);

  const getDateRange = useCallback(() => {
    const now = new Date();
    const endDate = now.toISOString();

    switch (dateRangePreset) {
      case '7d': {
        const startDate = new Date(now);
        startDate.setDate(startDate.getDate() - 7);
        return { startDate: startDate.toISOString(), endDate };
      }
      case '30d': {
        const startDate = new Date(now);
        startDate.setDate(startDate.getDate() - 30);
        return { startDate: startDate.toISOString(), endDate };
      }
      // ... (기타 케이스 생략, 동일 로직)
      case 'all':
      default:
        return undefined;
    }
  }, [dateRangePreset, customStartDate, customEndDate]);

  const fetchLinks = useCallback(async () => {
    const dateRange = getDateRange();
    const result = await getAllLinksForAnalytics(dateRange);
    if (result.success && result.data) {
      setLinks(showAll ? result.data : result.data.slice(0, 10));
    }
  }, [getDateRange, showAll]);

  useEffect(() => {
    fetchLinks();
  }, [fetchLinks]);

  const handleDelete = async (linkId: string) => {
    const result = await deleteLink(linkId);
    if (result.success) {
      setLinks((prev) => prev.filter(link => link.id !== linkId));
      toast.success("링크가 삭제되었습니다.");
    } else {
      toast.error(result.error || "링크 삭제에 실패했습니다.");
    }
  };

  const handleUpdateDescription = async (linkId: string, description: string) => {
    const { error } = await updateLinkDescription(linkId, description);
    if (error) {
      toast.error("설명 업데이트에 실패했습니다.");
      return;
    }
    setLinks((prev) => prev.map(l => l.id === linkId ? { ...l, description } : l));
    toast.success("설명이 업데이트되었습니다.");
  };

  const copyToClipboard = (text: string) => {
    navigator.clipboard.writeText(text)
      .then(() => toast.success("클립보드에 복사되었습니다."))
      .catch(() => toast.error("복사에 실패했습니다."));
  };

  return (
    <div className="space-y-4">
      {/* Header & Controls */}
      <div className="flex justify-between items-center">
        <h2 className="text-2xl font-bold">
          {showAll ? "모든 링크" : "상위 10개 링크"}
        </h2>
        <Button onClick={() => setShowAll(!showAll)}>
          {showAll ? "상위 10개만 보기" : "전체 보기"}
        </Button>
      </div>

      {/* Date Range Selector Panel (이전 코드와 동일, 생략 가능하지만 구조 보여줌) */}
      <Card className="border-none shadow-sm bg-white/50 dark:bg-white/5 backdrop-blur-sm">
        <CardContent className="p-4">
           {/* ... Date selection UI code ... */}
           <div className="flex items-center gap-2 text-sm font-medium mb-4">
              <Calendar className="h-4 w-4" />
              날짜 범위 선택
            </div>
            {/* Buttons for date range... */}
        </CardContent>
      </Card>

      {/* Links List */}
      <div className="grid gap-4">
        {links.map((link) => (
          <LinkItem
            key={link.id}
            link={link}
            baseUrl={baseUrl}
            isDateRangeAll={dateRangePreset === 'all'}
            onDelete={handleDelete}
            onUpdateDescription={handleUpdateDescription}
            onCopy={copyToClipboard}
            onShowStats={(l) => setSelectedLinkStatsId(selectedLinkStatsId === l.id ? null : l.id)}
            onAddToCampaign={(id) => {
                setSelectedLinkForCampaign(id);
                setShowAddToCampaign(true);
            }}
            renderStats={selectedLinkStatsId === link.id ? (
                <LinkStats linkId={link.id} dateRange={getDateRange()} />
            ) : undefined}
          />
        ))}
      </div>

      {/* Add to Campaign Dialog */}
      {selectedLinkForCampaign && (
        <AddToCampaignDialog
          open={showAddToCampaign}
          onOpenChange={setShowAddToCampaign}
          linkId={selectedLinkForCampaign}
          onSuccess={() => setSelectedLinkForCampaign(null)}
        />
      )}
    </div>
  );
}
```

---

### 결론 및 효과

이 리팩터링 계획을 순서대로 적용하면 다음과 같은 효과를 얻을 수 있습니다.

1.  **안전성 (Safety):** 새로운 타입(`Result`)과 스키마(`Zod`)를 먼저 정의하고, 이를 사용하는 함수를 점진적으로 수정하므로 기존 기능을 깨뜨리지 않고 리팩터링할 수 있습니다.
2.  **유지보수성 (Maintainability):** `LinkList`가 비대해지는 것을 막고, `LinkItem`으로 역할을 분리하여 코드를 읽기 쉽게 만듭니다. 또한, 검증 로직이 `schemas` 폴더에 중앙화되어 수정이 용이합니다.
3.  **테스트 용이성 (Testability):** 로직이 포함된 Server Action은 Zod 스키마 검증 덕분에 입력값에 대한 테스트가 명확해집니다. 컴포넌트 역시 역할이 분리되어 단위 테스트 작성이 수월해집니다.
